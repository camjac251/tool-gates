# Build artifacts
/target/
# /src/generated/ is committed so cargo package includes it (required for release-plz)
**/*.rs.bk

# mdBook build output (regenerated by `mdbook build docs`; deployed via Actions, not committed)
/docs/book/

# WASM simulator artifacts: built in CI (docs.yml) and locally via `mise run build-wasm`, not committed
/docs/src/wasm/tool_gates.js
/docs/src/wasm/tool_gates_bg.wasm

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
*.profraw
*.profdata
proptest-regressions/

# Local config
